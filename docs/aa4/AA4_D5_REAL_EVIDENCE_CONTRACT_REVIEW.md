# AA4-D5 real-evidence contract review

James O'Meara is asked to approve the proposed real-evidence acquisition contract in `contracts/authority_candidates/aa4/aa4_d5_real_evidence_acquisition_contract.candidate.json`.

The candidate provides the three missing decisions: a source-to-control map for the 13 AA4 controls, a read-only collector and canonical evidence-record contract, and fail-closed behavior. It is bound to `OscarMackJr/bluto` at immutable revision `7d3e12a2f7804041efd2840268f4d3d46f4266f9`.

To approve, open the pull request while signed in as `jo2082`, review the candidate, and choose **Approve**. Approval authorizes only publication of this exact contract and later static collector/readiness work. It does not authorize evidence collection, runtime activation, a one-run authorization, or an AA4 assessment.
